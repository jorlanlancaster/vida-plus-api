server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/vida_plus
    username: vida_user
    password: vida_pass
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
